package com.example.database;

public class Main {
    public static void main(String[] args) {
        // Get the instance of the DatabaseConnection singleton
        DatabaseConnection dbConnection = DatabaseConnection.getInstance();
        
        // Use the database connection methods
        // For example: dbConnection.connect();
    }
}
